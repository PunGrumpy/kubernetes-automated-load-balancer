apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-testapp.fullname" . }}
  labels:
    app: {{ template "helm-testapp.name" . }}
    chart: {{ template "helm-testapp.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-testapp.name" . }}
    release: {{ .Release.Name }}