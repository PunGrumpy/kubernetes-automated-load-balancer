Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{- if .Values.ingressRoute.enabled }}
1. Access your application:
   Your application should be available at:
   {{- range .Values.ingressRoute.routes }}
   {{- $match := .match }}
   {{- if contains "Host(`" $match }}
   {{- $host := regexReplaceAll "Host\\(`(.+)`\\).*" $match "${1}" }}
   https://{{ $host }}
   {{- end }}
   {{- end }}
   
   Note: It may take a few minutes for the DNS and TLS certificate to be fully propagated.

2. IngressRoute details:
   Name: {{ .Values.ingressRoute.name }}
   Entry Points: {{ join ", " .Values.ingressRoute.entryPoints }}
   TLS: Enabled (Cert Resolver: {{ .Values.ingressRoute.tls.certResolver }})
{{- else }}
1. Access your application:
   Your IngressRoute is not enabled. To access your application, you may need to expose it manually or enable the IngressRoute in your values.yaml file.
{{- end }}

3. Service details:
   Type: {{ .Values.service.type }}
   Port: {{ .Values.service.port }}

4. Autoscaling:
   Enabled: {{ .Values.autoscaling.enabled }}
   {{- if .Values.autoscaling.enabled }}
   Min Replicas: {{ .Values.autoscaling.minReplicas }}
   Max Replicas: {{ .Values.autoscaling.maxReplicas }}
   Target CPU Utilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}%
   Target Memory Utilization: {{ .Values.autoscaling.targetMemoryUtilizationPercentage }}%
   {{- end }}

-------------------------------------------------------------------------------

For more information and troubleshooting, please check the documentation or contact support.